# 交付物评审清单 🔍

> **AGENTS:** 在通过手动验证或自动化测试确认以下各项之前,不要把任何功能或任务标记为「完成」。请附上终端日志或浏览器测试结果作为证据。
> **人类评审者:** 在合并 Agent 生成的代码前,请使用本清单。

## 代码质量与安全
- [ ] 未使用 `any` 类型(或已用 `unknown` + type guard 严格说明理由)。
- [ ] 未经许可未修改受保护的文件 / 目录(如基础设施或迁移文件)。
- [ ] 未删除或跳过任何已有的、无关的测试。
- [ ] 组件 / 函数保持模块化,未粗暴破坏既有架构边界。

## 执行与测试
- [ ] 应用可编译,无致命错误。
- [ ] Linter 通过(后端 `ruff check .` / 前端 `npm run lint` 或等价命令)。
- [ ] 类型检查通过(前端 `tsc --noEmit` / 后端 type hints 或等价检查)。
- [ ] 相关的单元 / 集成测试通过。
- [ ] **幂等性与中断恢复**:处理中途 kill、重启后无重复副作用(本项目最高优先级)。
- [ ] UI 在桌面视口下表现正常(本项目仅桌面浏览器端,不做移动端)。

## 安全 🔐
- [ ] diff 中任何位置都没有硬编码的密钥、API key 或 token。
- [ ] `.env`(及任何其他密钥文件)、`tenants/`、`data/private/` 已 gitignore 且未被提交。
- [ ] 依赖已审计(`npm audit` / `pip-audit` 或等价工具)—— 无未处理的高危项。
- [ ] 所有用户输入在边界处已校验与净化(表单、API 载荷、URL 参数);后端用 Pydantic、前端用 Zod。
- [ ] 受认证保护的路由与操作已在登出状态下测试;RBAC 三角色权限边界正确。
- [ ] 公开端点已考虑限流(或等价的滥用防护);CORS 白名单、安全响应头已接入。
- [ ] **脱敏一致性**:同一主体在不同行映射为同一 token(否则跨行关联检测失效)。
- [ ] **PII 保护**:真实 PII 未经脱敏不得进入云端 LLM;prompt 注入红队(promptfoo)已通过。
- [ ] **引用忠实性**:LLM 产出的制度条款引用已通过机械式逐字校验方才呈现。

## 交付物交接
- [ ] `MEMORY.md` 已更新本次任务中做出的任何新架构决策。
- [ ] 工作区中已过时的 spec 文件已标记为已解决或归档。
